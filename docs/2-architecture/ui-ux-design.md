# F6 대시보드 UI/UX 설계 문서

> **작성일:** 2026-03-17
> **기반 설계:** ADR-006 (React 18 + Vite), ui-design.md
> **목적:** 화면 구성 의사결정 근거, UX 고민 과정, 최종 설계 결론을 기록

---

## 1. 사용자 분석 (Who are we designing for?)

관제 시스템을 실제로 사용하는 사람이 누구인지 먼저 정의했다.
사용자가 달라지면 정보 배치와 우선순위가 완전히 달라지기 때문이다.

| 사용자 | 업무 상황 | 핵심 질문 | 화면에서 가장 먼저 봐야 하는 것 |
|--------|----------|-----------|-------------------------------|
| 현장 작업자 / 기술자 | 설비 옆에서 몸으로 일하면서 화면을 흘끗 봄. 장갑 낀 손으로 터치 가능성 | **지금 뭐가 문제인가?** | 설비 상태 색상 (빨강/초록) |
| 정비 엔지니어 | 알람 받고 대응 판단. 무엇을 어떻게 고쳐야 하는지 즉시 파악 필요 | **무엇을 어떻게 고쳐야 하나?** | LLM 조치 단계 + 필요 부품 |
| 생산 관리자 | 전체 라인 흐름 모니터링. 납기 리스크 판단 | **3대가 다 돌아가고 있나?** | 설비 3대 한눈에 + 작업 납기 |

**설계 원칙으로 도출:**
- 가장 급한 정보(Critical 알람)가 화면 진입 즉시 보여야 한다
- 텍스트보다 색상과 배지로 상태를 먼저 전달한다
- 상세 정보는 한 단계 더 들어가서 볼 수 있도록 계층 구성

---

## 2. 초안 목업 (v1) 의 문제점

첫 번째 목업은 **Overview 페이지 → 설비 상세 페이지** 구조(2-Page SPA)였다.
검토 결과 아래 문제가 발견되었다.

### 2-1. 페이지 이동 방식의 한계

```
Overview (설비 카드 3개)
  └─ 클릭 → 상세 페이지 (전체 교체)
      └─ 뒤로가기 → Overview
```

| 문제 | 설명 |
|------|------|
| 맥락 손실 | 상세 페이지로 이동하면 다른 2대의 상태가 안 보임 |
| 인지 부하 | 현장에서 "뒤로가기" 버튼을 찾아 누르는 행위 자체가 불필요한 부담 |
| 비교 불가 | CNC-001과 CNC-002를 동시에 비교할 방법이 없음 |

### 2-2. 기능 누락 목록

v1 목업에서 설계는 되어 있었으나 화면에 반영되지 않은 기능들:

| 누락 기능 | 연결 API | 중요도 |
|-----------|---------|--------|
| F4 GraphRAG 참조 문서 (DOC-004 등) | `/api/f4/search` | High — LLM 판단 근거 가시화 |
| F2 이상 점수 히스토리 차트 | `/api/f2/history` | High — "언제부터 올라갔나?" |
| F1~F5 파이프라인 상태 | `/api/health` 확장 | Medium — 백엔드 정상 동작 확인 |
| STOP/REDUCE 임계치 선 | 클라이언트 상수 | High — 숫자보다 선이 직관적 |
| 정비 이력 타임라인 | `/api/f6/maintenance/:id` | Medium — 과거 패턴 맥락 |
| 알람 확인(Acknowledge) 버튼 | `POST /api/f6/alarms/:id/acknowledge` | Medium — 운영 감사 추적 |
| 다중 KPI 카드 | `/api/f6/summary` | Medium — 핵심 지표 한눈에 |

---

## 3. UX 패턴 선택: 3-Pane Layout

### 3-1. 선택지 비교

| 방식 | 장점 | 단점 | 채택 |
|------|------|------|------|
| **2-Page SPA** (v1) | 구현 단순 | 맥락 손실, 뒤로가기 필요 | ❌ |
| **탭 방식** | 같은 페이지 유지 | 탭 전환 시 여전히 맥락 분리 | ❌ |
| **3-Pane Layout** | 페이지 이동 없음, 모든 맥락 동시 표시 | 화면 폭 필요 (1280px+) | ✅ |
| **Overlay / Drawer** | 메인 화면 유지 | 드로어가 내용 가림 | 보조 용도로만 |

### 3-2. 3-Pane 구조

```
┌──────────────┬──────────────────────────────┬────────────────────────┐
│  LEFT 240px  │  CENTER (flex-1)             │  RIGHT 360px           │
│              │                              │                        │
│  설비 목록   │  선택된 설비의 주요 데이터   │  AI 조치 + 근거        │
│  ──────────  │                              │                        │
│  ● CNC-001  │  [Critical 배너]             │  [이상 점수 게이지]    │
│  🔴 CNC-002  │  [KPI 카드 4개]              │  [STOP/REDUCE/MONITOR] │
│  ● CNC-003  │  [실시간 센서 차트]          │  [조치 단계]           │
│              │  [이상 점수 히스토리]        │  [GraphRAG 문서 3건]   │
│  ──────────  │  [정비 이력 타임라인]        │  [필요 부품 + 재고]    │
│  알람 피드   │                              │  [작업 현황]           │
│  ──────────  │                              │                        │
│  파이프라인  │                              │                        │
│  F1●F2●F3●  │                              │                        │
│  F4●F5●F6●  │                              │                        │
└──────────────┴──────────────────────────────┴────────────────────────┘
```

### 3-3. 인터랙션 흐름

```
화면 진입
  → 가장 Critical한 설비(CNC-002)가 자동 선택됨
  → Center: CNC-002 센서/이상 데이터 표시
  → Right: LLM 조치 리포트 표시
  → 왼쪽 리스트에서 다른 설비 클릭
      → 페이지 이동 없이 Center + Right만 교체
      → Left 목록은 유지 (비교 맥락 살아있음)
```

---

## 4. 디자인 시스템: Palantir Blueprint

### 4-1. 선택 근거

| 기준 | 이유 |
|------|------|
| 정보 밀도 | 제조 관제는 소비자 앱이 아님. 전문가용 고밀도 UI가 맞음 |
| 다크 테마 | 공장 현장 환경 특성. 장시간 모니터링에 눈 부담 낮음 |
| 시맨틱 색상 | 색상이 장식이 아닌 의미를 가짐 (빨강=위험, 초록=정상) |
| 산업용 레퍼런스 | Palantir Foundry/AIP가 실제 제조·에너지·국방 현장에서 사용 |

### 4-2. 컬러 토큰 (Blueprint Dark Theme)

```
배경 레이어 (3단계 깊이):
  --black:  #111418   최하층 배경 (body)
  --dg1:    #1C2127   메인 배경 (panel)
  --dg2:    #252A31   카드 배경
  --dg3:    #2F343C   카드 hover / 인풋 배경
  --dg4:    #383E47   인풋, 비활성

텍스트:
  --gray5:  #C5CBD3   주요 텍스트
  --gray3:  #8F99A8   보조 레이블
  --gray1:  #5F6B7C   비활성 / 타임스탬프

강조색 (단 하나):
  --blue3:  #2D72D2   인터랙티브 요소, 선택 상태
  --blue5:  #8ABBFF   다크 배경 위 파란색

상태 색상 (시맨틱):
  --green5: #72CA9B   정상 (OK)
  --orange5:#FBB360   경고 (REDUCE)
  --red5:   #FA999C   위험 (STOP)
  --gold5:  #FFE39F   예측 고장코드 하이라이트
```

### 4-3. Palantir 시각적 특징 (구현 포인트)

| 특징 | 구현 방법 |
|------|-----------|
| **대문자 소형 섹션 레이블** | `font-size: 11px; font-weight: 700; letter-spacing: 0.08em; text-transform: uppercase` |
| **직각에 가까운 모서리** | `border-radius: 2~3px` (소비자앱의 둥근 모서리 대신) |
| **얇은 구분선** | `border: 1px solid rgba(255,255,255,0.08)` |
| **왼쪽 4px 컬러 보더** | 알람/콜아웃 컴포넌트의 Blueprint 시그니처 패턴 |
| **모노스페이스 숫자/ID** | `font-family: "SFMono-Regular", Consolas, monospace` |
| **차트 그리드 최소화** | `rgba(255,255,255,0.06)` — 거의 안 보임 |
| **임계치 점선** | 주황 점선 수평선으로 STOP(0.8) / REDUCE(0.6) 표시 |

---

## 5. 정보 계층 설계 (Information Hierarchy)

사용자가 화면을 봤을 때 어떤 순서로 정보를 인식하는지 설계했다.

### 5-1. Left Panel — "어떤 설비가 문제인가?"

```
우선순위:
  1. 빨간 점 + "위험" 배지 → 즉시 시선 포착
  2. anomaly_score 수치
  3. 설비명 (CNC-001/002/003)
  4. 알람 피드 (최근 이상 목록)
  5. 파이프라인 상태 (F1~F6 도트)
```

**자동 포커스:** 페이지 진입 시 가장 높은 anomaly_score 설비가 자동 선택됨.
Critical 설비가 없으면 score 높은 순으로 선택.

### 5-2. Center Panel — "무슨 일이 일어나고 있는가?"

```
우선순위:
  1. Critical 배너 (score ≥ 0.8일 때만 표시) — 화면 최상단
  2. KPI 카드 4개 (이상점수, 고장코드, 확신도, 다운타임)
  3. 실시간 센서 차트 (이상 구간 빨간 하이라이트)
  4. 이상 점수 히스토리 (언제부터 올라갔는지)
  5. 정비 이력 타임라인 (과거에도 이런 일이 있었는지)
```

### 5-3. Right Panel — "무엇을 해야 하는가?"

```
우선순위:
  1. 이상 점수 게이지 (현재 상태 한눈에)
  2. STOP/REDUCE/MONITOR 배너 (행동 지시)
  3. 조치 단계 (번호 리스트, 즉시 실행 가능)
  4. 판단 근거 (왜 이 조치인가)
  5. GraphRAG 참조 문서 (어떤 매뉴얼을 참고했는가)
  6. 필요 부품 + 재고 현황
  7. 작업 현황 (납기 영향 판단)
```

---

## 6. 알람 처리 UX

### 6-1. 심각도 3단계

| 레벨 | 조건 | 시각 표현 | 사운드 |
|------|------|-----------|--------|
| **CRITICAL** | anomaly_score ≥ 0.8 | 빨간 배너 (전체 폭) + 도트 깜빡임 | Phase 3 결정 |
| **WARNING** | 0.6 ≤ score < 0.8 | 주황 배지 + 알람 피드 | 없음 |
| **MONITOR** | 0.5 ≤ score < 0.6 | 노란 배지 (알람 피드만) | 없음 |

### 6-2. Critical 배너 UX

- 화면 최상단 고정 (스크롤해도 보임)
- "확인 처리" 버튼 → `POST /api/f6/alarms/:id/acknowledge` 호출
- 확인 후 배너 색이 흐려지고 "확인됨 ✓" 표시
- **Phase 3 구현:** 확인자 + 시각 기록 (Audit trail)

### 6-3. 알람 자동 선택 로직

```
페이지 진입 시:
  if (CRITICAL 설비 있음):
    → 해당 설비 자동 선택 + Critical 배너 표시
  else if (WARNING 설비 있음):
    → score 가장 높은 설비 자동 선택
  else:
    → CNC-001 기본 선택
```

---

## 7. 폴링 전략 (3-Pane 기준)

3-Pane 구조에서는 "전체 목록"과 "선택된 설비 상세"의 갱신 주기를 다르게 가져간다.

| 데이터 | 주기 | 위치 | 이유 |
|--------|------|------|------|
| Left 설비 목록 + 상태 | 5초 | Left | 항상 최신 상태 유지 |
| Left 알람 피드 | 5초 | Left | 새 알람 즉시 반영 |
| Left 파이프라인 도트 | 10초 | Left | 헬스 체크 |
| Center KPI 카드 | 5초 | Center | 실시간 점수 |
| Center 센서 차트 | 5초 | Center | 실시간 차트 |
| Center 이상 점수 히스토리 | 5초 | Center | 추이 갱신 |
| Center 정비 이력 | 60초 | Center | 자주 안 바뀜 |
| Right 이상 점수 게이지 | 5초 | Right | 실시간 |
| Right LLM 리포트 | 이벤트 | Right | 알람 시에만 생성 |
| Right GraphRAG 문서 | 이벤트 | Right | 알람과 함께 |
| Right 부품/재고 | 30초 | Right | ERP 데이터 |
| Right 작업 현황 | 30초 | Right | MES 데이터 |

---

## 8. GraphRAG 결과 표시 설계

GraphRAG(F4)는 LLM이 왜 이 판단을 했는지 근거를 가시화하는 핵심 기능이다.
단순히 "AI가 말했다"가 아니라 "어떤 매뉴얼의 어떤 내용을 참고해서 판단했다"를 보여준다.

### 8-1. 표시 정보

```
┌──────────────────────────────────────────┐
│  DOC-004          hybrid score: 0.94     │
│  SPINDLE_OVERHEAT_001 교체절차서          │
│  "스핀들 과열 발생 시 즉시 전원 차단 후   │
│   냉각 대기. 베어링 마모 확인 후 P002…"  │
└──────────────────────────────────────────┘
```

- `manual_id` — 문서 식별자 (DOC-004)
- `title` — 문서 제목
- `hybrid_score` — BM25 + Vector 혼합 점수
- `snippet` — 관련 청크 텍스트 200자 (api-design.md에 필드 추가됨)

### 8-2. API 변경 사항

`RelatedDocument` Pydantic 모델에 `snippet` 필드 추가:
```python
class RelatedDocument(BaseModel):
    manual_id: str
    title: str
    hybrid_score: float
    bm25_score: float
    vector_score: float
    text_preview: Optional[str] = None
    snippet: Optional[str] = None   # UI 표시용 (최대 200자)
```

---

## 9. 정비 이력 타임라인 설계

과거 정비 이력을 타임라인 형태로 보여줌으로써
현재 이상 상황을 과거 패턴과 연결해 해석할 수 있게 한다.

```
●── 2주 전   [수리]  SPINDLE_OVERHEAT_001
│            P002 베어링 교체 · 소요: 72분
│
●── 3주 전   [수리]  TOOL_WEAR_001
│            P001 Endmill 교체 · 소요: 28분
│
●── 1개월 전 [예방]  정기 점검
             윤활 · 청소 · 소모품 교체
```

**신규 API:** `GET /api/f6/maintenance/{equipment_id}` (60초 폴링)

---

## 10. 미결 / Phase 3 결정 사항

| # | 항목 | 현재 상태 | 결정 시점 |
|---|------|-----------|-----------|
| UX-1 | LLM 리포트 스트리밍 (SSE) vs 완료 후 표시 | Open | Phase 3 |
| UX-2 | 알람 사운드 / 브라우저 Push 알림 | Open | Phase 3 |
| UX-3 | 이상 이력 기간 선택 (1h / 6h / 24h) | Open | Phase 3 |
| UX-4 | 모바일 반응형 (현장 태블릿) | Open | MVP 이후 |
| UX-5 | 알람 확인(Acknowledge) 권한 관리 | Open | Phase 3 |
| UX-6 | Critical 알람 시 Center 단순화 모드 | 설계 완료, 구현 예약 | Phase 3 |
| UX-7 | 다중 센서 동시 비교 (오버레이) | Open | Phase 3 |
| UX-8 | 설비 간 이상 점수 비교 차트 | Open | Phase 3 이후 |

---

## 11. 목업 버전 이력

| 버전 | 날짜 | 주요 변경 |
|------|------|-----------|
| v1 | 2026-03-16 | 2-Page SPA, 기본 레이아웃, 핵심 기능 일부 누락 |
| v2 | 2026-03-17 | 3-Pane Layout, Palantir Blueprint 스타일, 전체 기능 포함 (GraphRAG, 파이프라인 상태, 정비 이력, 임계치 선, Acknowledge) |

---

## 리뷰 피드백 기록

### 리뷰 #1 — 백엔드 Claude (2026-03-17)

1. **3-Pane Layout 동의** — 백엔드 `/api/f6/summary`에서 score 높은 순 정렬로 자동 포커스 지원 가능 ✅
2. **GraphRAG snippet 필드 누락** — `RelatedDocument`에 `snippet` 추가 필요 → api-design.md 반영 ✅
3. **정비 이력 엔드포인트 없음** — `/api/f6/maintenance/{eq_id}` 신규 추가 → api-design.md 반영 ✅
4. **Acknowledge = POST 엔드포인트** — `POST /api/f6/alarms/:id/acknowledge` Phase 3 예약 ✅
5. **폴링 전략 차별화** — 설비 목록 5초 / 정비 이력 60초 / LLM 리포트 이벤트 → 반영 ✅
6. **Critical 단순화 모드** — Critical 알람 시 Center 영역이 단순해지면 좋겠다 → UX-6으로 등록, Phase 3 구현 예약 ✅
